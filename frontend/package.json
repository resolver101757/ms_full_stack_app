{
  "dependencies": {
    "axios": "^1.7.4"
  },
  "devDependencies": {
    "@quasar/cli": "^2.4.1",
    "concurrently": "^8.2.2",
    "json-server": "^1.0.0-beta.2"
  },
  "scripts": {
    "dev": "quasar dev",
    "build": "quasar build",
    "mock-api": "json-server --watch db.json --port 3000",
    "start": "concurrently \"npm run dev\" \"npm run mock-api\""
  }
}
